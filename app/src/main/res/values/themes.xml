<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EventosCompartidos" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="btn_basic_style" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/txt_color_textBox</item>
    </style>

    <style name="textBox_basic_style" parent="TextAppearance.AppCompat">
        <item name="android:background">@color/bk_textBox_</item>
        <item name="android:textColor">@color/txt_color_textBox</item>
        <item name="cornerRadius">10dp</item>
    </style>

    <style name="Theme.EventosCompartidos.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.EventosCompartidos.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.EventosCompartidos.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>